<htnl>

    <head>
        <title>handlebars demo</title>
    </head>

    <body>
        <h1>
            {{title}}
        </h1>
        <ul>
            {{#each list}}
            <li>{{this}}</li>
            {{/each}}
        </ul>
    </body>
</htnl>